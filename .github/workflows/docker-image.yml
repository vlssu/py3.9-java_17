name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: 构建 Docker 镜像
      run: docker build . --file Dockerfile --tag py3.9-java_17:$(date +%s)
